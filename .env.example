# 이 파일은 "형식만" 담은 예시다. 실제 키를 여기에 적지 않는다 — 커밋된다.
# 복사: cp .env.example .env.local   (.env.local 은 .gitignore 로 커밋되지 않는다)

# https://aistudio.google.com/apikey 에서 발급
GEMINI_API_KEY= 

# 선택. 비워두면 gemini-3.5-flash-lite (무료 티어 한도가 가장 넉넉하고 가장 빠름).
# 사진 OCR 정확도가 부족하면 gemini-3.6-flash 로 올린다.
GEMINI_MODEL= 
