# 개발 분담 — 2026-08-13 본선

3인 병렬. **자기 칸 밖의 파일은 건드리지 않는다.** 충돌은 전부 접점에서 난다.

## 1. 담당 범위

### 순범 (리드)
- 화물 정보 탭 「선호 지역 설정」에 **운행 출발지 · 운행 복귀지** 두 칸 추가
  → AI 경로 추천이 이 두 칸에 바로 꽂힌다. 기사 운행 맥락 파악의 기준점.
- 각 콜의 **업무 외 대기시간**(대기 + 상하차) 추정 → 이를 바탕으로 **실질 시급** 산출
  → 화물 정보 카드에 표기. 같은 표기가 스케줄 탭 「AI 오더 추천」 카드에도 들어간다.
- 스케줄 탭 **「외부 오더 등록」** — 텍스트·사진 파싱해서 콜 등록
- 공통 계약(`types.ts`) · 공유 저장소 · 머지 전담

### 동의
- **AI 리포트 전부**
- **월간 보기**

### 지수
- **스케줄 탭 전부 (주간 보기)**

## 2. 폴더 구분

```
src/
├─ lib/
│  ├─ types.ts                     [공통]  바꾸려면 순범에게 말한다. 혼자 고치지 않는다
│  ├─ store/                       [순범]  ★ 세 사람이 공유하는 상태. 읽기/호출만 한다
│  │   ├─ AppStateProvider.tsx             useAppState() 훅
│  │   ├─ settings-store.ts                선호지역 + 운행 출발지·복귀지
│  │   └─ schedule-store.ts                캘린더에 꽂힌 오더
│  ├─ engine/                      [순범]  대기시간 추정 · 실질시급 · 원가 파라미터
│  ├─ ai/
│  │   ├─ client.ts                [순범]  Gemini 호출 래퍼 (동의도 이걸 쓴다)
│  │   ├─ parse-order.ts           [순범]  오더 파싱 프롬프트
│  │   └─ report.ts                [동의]  리포트 프롬프트
│  └─ mock/                        [공통]  합성 데이터. 넣기 전에 단톡에 한 줄 남긴다
│
├─ app/
│  ├─ layout.tsx                   [순범]  Provider 마운트
│  ├─ cargo/**                     [순범]  화물 정보 탭
│  ├─ settings/location/**         [순범]  선호 지역 + 운행 출발지·복귀지
│  ├─ schedule/page.tsx            [지수]  스케줄 탭 껍데기
│  └─ api/
│      ├─ parse-order/route.ts     [순범]
│      └─ report/route.ts          [동의]
│
└─ components/
   ├─ GNB.tsx                      [순범]
   ├─ common/CallMetrics.tsx       [순범]  ★ 실질시급·업무외대기 표기. 지수가 import
   ├─ order-import/**              [순범]  외부 오더 등록 버튼 + 시트
   ├─ cargo/**                     [순범]
   ├─ schedule/**                  [지수]  주간 보기 · 타임라인 · 빈칸 · 추천 카드
   ├─ calendar/**                  [동의]  월간 보기
   └─ report/**                    [동의]  AI 리포트
```

## 3. 접점 세 곳 — 여기만 조심하면 안 부딪힌다

### ① 스케줄 탭에 순범의 「외부 오더 등록」이 들어간다
지수는 `src/app/schedule/page.tsx` 에서 이 한 줄만 쓴다.
```tsx
<ExternalOrderButton />
```
버튼·시트·파싱은 전부 `src/components/order-import/` 안에서 끝난다.
등록 결과는 공유 저장소로 들어가므로 지수의 주간 보기와 동의의 월간 보기에 **자동으로 같이 뜬다.**

### ② 「AI 오더 추천」 카드의 실질시급 표기
지수가 카드를 만들고, **숫자는 순범의 컴포넌트가 찍는다.**
```tsx
<CallMetrics order={order} compact />
```
카드마다 따로 숫자를 그리면 화물정보 탭과 스케줄 탭의 값이 갈라진다. 그 순간 심사에서 무너진다.

### ③ AI 리포트 → 운행 출발지·복귀지
동의는 리포트에서 이 호출 하나만 한다.
```tsx
applyRecommendedRoute({ start, end })
```
그러면 순범의 설정 화면 두 칸이 바뀌고 화물 정보 목록이 그 자리에서 다시 정렬된다.
**설정 화면 파일을 직접 고치지 않는다.**

> 셋 다 공통점이 같다 — **남의 화면을 고치는 게 아니라, 내 컴포넌트/함수를 한 줄 넘긴다.**

## 4. 브랜치와 머지

```
main
├─ feat/sunbeom-*    순범
├─ feat/dongui-*     동의
└─ feat/jisu-*       지수
```
- 머지는 **순범이 전담**한다. 동의·지수는 push 만 하고 직접 머지하지 않는다.
- 1시간마다 한 번씩 `main` 으로 올린다. 몰아서 하면 15:30에 터진다.
- `types.ts` 나 `store/` 를 바꿔야 하면 **먼저 말한다.** 두 사람 빌드가 같이 깨지는 유일한 파일들이다.

## 5. 규칙 셋

1. **숫자를 화면에서 만들지 않는다.** 금액·시간은 `src/lib/engine` 산출값을 받아 쓴다.
2. **표본 수를 같이 쓴다.** `1.4h` 만 쓰면 숫자고 `(18건)` 을 붙이면 근거다. 없으면 "기록 없음"이라고 쓴다.
3. **못 읽은 값은 비운다.** 상식으로 메우면 틀린 하루가 만들어진다. "미기재"라고 쓴다.

## 6. 쓰지 않는 말

| ✗ | ○ |
|---|---|
| 자동 배차 | 배차 조건 / 알림 조건 |
| 이 오더를 추천드립니다 | (순서만 바꾼다) |
| 복귀 화물을 찾아드립니다 | 집 방향 건이 위로 올라옵니다 |
| 피로도 78점 | 연속운전 2시간 40분 |
| 법 위반입니다 | 휴게가 필요한 구간이 생깁니다 |

## 7. 타임라인

| 시각 | |
|---|---|
| ~11:30 | 각자 브랜치 생성 · 첫 배포 |
| 11:30~15:30 | 병렬 개발. 1시간마다 머지 |
| **15:30** | **기능 동결.** 이후 커밋은 문구·데이터만 |
| 15:30~18:00 | 데모 영상 · 결과보고서 · 최종 배포 |

---
`dev-log/` 는 `.gitignore` 에 걸려 있다. 각자 로컬에만 남고 커밋되지 않는다.
